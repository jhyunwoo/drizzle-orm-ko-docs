import CodeWithProps from "@mdx-ko/CodeWithProps.astro";

프로젝트 루트에 `.env` 파일을 만들고 데이터베이스 연결 변수를 추가하세요:

<CodeWithProps env_variable={props.env_variable}>
```plaintext copy
$env_variable$=
```
</CodeWithProps>
