import Tab from '@mdx-ko/Tab.astro';
import Tabs from '@mdx-ko/Tabs.astro';
import Npm from "@mdx-ko/Npm.astro";
import Callout from '@mdx-ko/Callout.astro';
import Steps from '@mdx-ko/Steps.astro';
import AnchorCards from '@mdx-ko/AnchorCards.astro';
import Breadcrumbs from '@mdx-ko/Breadcrumbs.astro';
import Prerequisites from "@mdx-ko/Prerequisites.astro";
import CodeTabs from "@mdx-ko/CodeTabs.astro";
import FileStructure from '@mdx-ko/get-started/FileStructure.mdx';
import InstallPackages from '@mdx-ko/get-started/InstallPackages.mdx';
import ConnectEffect from '@mdx-ko/get-started/postgresql/ConnectEffect.mdx'
import CreateTable from '@mdx-ko/get-started/postgresql/CreateTable.mdx'
import SetupConfig from '@mdx-ko/get-started/SetupConfig.mdx';
import ApplyChanges from '@mdx-ko/get-started/ApplyChanges.mdx';
import RunFile from '@mdx-ko/get-started/RunFile.mdx';
import QueryDatabase from '@mdx-ko/get-started/QueryDatabase.mdx';
import SetupEnv from '@mdx-ko/get-started/SetupEnv.mdx';

<Breadcrumbs/>

# Drizzle과 Effect PostgreSQL로 시작하기

<Callout type='error'>
이 페이지는 drizzle `1.0.0-beta.13` 이상 버전에서 사용할 수 있는 개념을 설명합니다.

Effect는 현재 PostgreSQL에서만 사용할 수 있으며, 곧 다른 모든 dialect에도 구현될 예정입니다.

업그레이드 방법은 [여기](/docs/upgrade-v1)에서 더 자세히 읽어보세요.
</Callout>

<Prerequisites>
  - **Effect** - 복잡한 동기 및 비동기 프로그램을 쉽게 만들 수 있도록 설계된 강력한 TS 라이브러리입니다. - [더 읽기](https://effect.website/docs)
  - **dotenv** - 환경 변수를 관리하는 패키지 - [여기에서 읽기](https://www.npmjs.com/package/dotenv)
  - **tsx** - TypeScript 파일을 실행하는 패키지 - [여기에서 읽기](https://tsx.is/)
  - **@effect/sql-pg** - Effect용 PostgreSQL 툴킷 - [여기에서 읽기](https://effect-ts.github.io/effect/docs/sql-pg)
</Prerequisites>

Drizzle은 `@effect/sql-pg` 드라이버를 사용하는 Effect PostgreSQL 연결을 기본적으로 지원합니다.

<FileStructure/>

#### 1단계 - 필요한 패키지 설치
<InstallPackages lib='effect @effect/sql-pg pg' devlib=' @types/pg'/>

#### 2단계 - 연결 변수 설정

<SetupEnv env_variable='DATABASE_URL' />

<Callout title='팁'>
아직 PostgreSQL 데이터베이스가 없고 테스트용으로 하나 만들고 싶다면, PostgreSQL을 Docker에 설정하는 가이드를 사용할 수 있습니다.

PostgreSQL in Docker 가이드는 [여기](/docs/guides/postgresql-local-setup)에서 볼 수 있습니다. 설정을 마친 뒤 데이터베이스 URL을 생성하고(가이드에 설명되어 있습니다), 다음 단계로 돌아오세요.
</Callout>

#### 3단계 - Drizzle ORM을 데이터베이스에 연결

<ConnectEffect />

#### 4단계 - 테이블 생성

<CreateTable />

#### 5단계 - Drizzle config 파일 설정

<SetupConfig dialect='postgresql' env_variable='DATABASE_URL'/>

#### 6단계 - 데이터베이스에 변경 사항 적용

<ApplyChanges />

#### 7단계 - 데이터베이스 시드 및 쿼리

사용자를 생성, 조회, 수정, 삭제하는 쿼리를 넣도록 `src/index.ts` 파일을 **업데이트**해 봅시다

```ts
import 'dotenv/config';
import * as PgDrizzle from 'drizzle-orm/effect-postgres';
import { PgClient } from '@effect/sql-pg';
import * as Effect from 'effect/Effect';
import * as Redacted from 'effect/Redacted';
import { types } from 'pg';
import { eq } from 'drizzle-orm';
import { usersTable } from './db/schema';

const PgClientLive = PgClient.layer({
  url: Redacted.make(process.env.DATABASE_URL!),
  types: {
    getTypeParser: (typeId, format) => {
      if ([1184, 1114, 1082, 1186, 1231, 1115, 1185, 1187, 1182].includes(typeId)) {
        return (val: any) => val;
      }
      return types.getTypeParser(typeId, format);
    },
  },
});

const program = Effect.gen(function*() {
  const db = yield* PgDrizzle.makeWithDefaults();

  const user: typeof usersTable.$inferInsert = {
    name: 'John',
    age: 30,
    email: 'john@example.com',
  };

  yield* db.insert(usersTable).values(user);
  console.log('New user created!')

  const users = yield* db.select().from(usersTable);
  console.log('Getting all users from the database: ', users)
  /*
  const users: {
    id: number;
    name: string;
    age: number;
    email: string;
  }[]
  */

  yield* db
    .update(usersTable)
    .set({
      age: 31,
    })
    .where(eq(usersTable.email, user.email));
  console.log('User info updated!')

  yield* db.delete(usersTable).where(eq(usersTable.email, user.email));
  console.log('User deleted!')
});

Effect.runPromise(program.pipe(Effect.provide(PgClientLive)));
```

#### 8단계 - index.ts 파일 실행

<RunFile/>
