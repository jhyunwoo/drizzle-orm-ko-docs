import Tab from '@mdx-ko/Tab.astro';
import Tabs from '@mdx-ko/Tabs.astro';
import Npm from "@mdx-ko/Npm.astro";
import Callout from '@mdx-ko/Callout.astro';
import Steps from '@mdx-ko/Steps.astro';
import AnchorCards from '@mdx-ko/AnchorCards.astro';
import Breadcrumbs from '@mdx-ko/Breadcrumbs.astro';
import CodeTabs from "@mdx-ko/CodeTabs.astro";
import Prerequisites from "@mdx-ko/Prerequisites.astro";
import IntrospectSQLite from '@mdx-ko/get-started/sqlite/IntrospectSqlite.mdx';
import FileStructure from '@mdx-ko/get-started/FileStructure.mdx';
import InstallPackages from '@mdx-ko/get-started/InstallPackages.mdx';
import SetupConfig from '@mdx-ko/get-started/SetupConfig.mdx';
import SetupEnv from '@mdx-ko/get-started/SetupEnv.mdx';
import TransferCode from '@mdx-ko/get-started/TransferCode.mdx';
import ApplyChanges from '@mdx-ko/get-started/ApplyChanges.mdx';
import RunFile from '@mdx-ko/get-started/RunFile.mdx';
import ConnectSQLiteCloud from '@mdx-ko/get-started/sqlite/ConnectSQLiteCloud.mdx';
import QueryDatabase from '@mdx-ko/get-started/QueryDatabase.mdx';
import QueryDatabaseUpdated from '@mdx-ko/get-started/QueryDatabaseUpdated.mdx';
import UpdateSchema from '@mdx-ko/get-started/sqlite/UpdateSchema.mdx';

<Breadcrumbs/>

# 기존 프로젝트에서 Drizzle과 SQLite Cloud 시작하기

<Prerequisites>
  - **dotenv** - 환경 변수를 관리하기 위한 패키지 - [여기에서 읽기](https://www.npmjs.com/package/dotenv)
  - **tsx** - TypeScript 파일을 실행하기 위한 패키지 - [여기에서 읽기](https://tsx.is/)
  - **SQLite Cloud database** - [여기에서 읽기](https://docs.sqlitecloud.io/docs/overview)
  - **SQLite Cloud driver** - [여기에서 읽기](https://docs.sqlitecloud.io/docs/sdk-js-introduction) & [GitHub](https://github.com/sqlitecloud/sqlitecloud-js)
</Prerequisites>

<FileStructure/>

#### Step 1 - 필요한 패키지 설치하기
<Npm>
  drizzle-orm@beta @sqlitecloud/drivers dotenv
  -D drizzle-kit@beta tsx
</Npm>

#### Step 2 - 연결 변수 설정하기

<SetupEnv env_variable='SQLITE_CLOUD_CONNECTION_STRING' />

#### Step 3 - Drizzle config 파일 설정하기

<SetupConfig dialect='sqlite' env_variable='SQLITE_CLOUD_CONNECTION_STRING'/>

#### Step 4 - 데이터베이스 introspect 하기

<IntrospectSQLite/>

#### Step 5 - 실제 스키마 파일로 코드 옮기기

<TransferCode/>

#### Step 6 - 데이터베이스에 Drizzle ORM 연결하기

<ConnectSQLiteCloud/>

#### Step 7 - 데이터베이스 조회하기

```typescript copy filename="src/index.ts"
import 'dotenv/config';
import { eq } from 'drizzle-orm';
import { drizzle } from 'drizzle-orm/sqlite-cloud';
import { usersTable } from './db/schema';

async function main() {
  const db = drizzle();

  const user: typeof usersTable.$inferInsert = {
    name: 'John',
    age: 30,
    email: 'john@example.com',
  };

  await db.insert(usersTable).values(user);
  console.log('New user created!')

  const users = await db.select().from(usersTable);
  console.log('Getting all users from the database: ', users)
  /*
  const users: {
    id: number;
    name: string;
    age: number;
    email: string;
  }[]
  */

  await db
    .update(usersTable)
    .set({
      age: 31,
    })
    .where(eq(usersTable.email, user.email));
  console.log('User info updated!')

  await db.delete(usersTable).where(eq(usersTable.email, user.email));
  console.log('User deleted!')
}

main();
```

#### Step 8 - index.ts 파일 실행하기

<RunFile/>

#### Step 9 - 테이블 스키마 업데이트하기 (선택 사항)

<UpdateSchema/>

#### Step 10 - 데이터베이스에 변경 사항 적용하기 (선택 사항)

<ApplyChanges />

#### Step 11 - 새 필드로 데이터베이스 조회하기 (선택 사항)

```typescript copy filename="src/index.ts"
import 'dotenv/config';
import { eq } from 'drizzle-orm';
import { drizzle } from 'drizzle-orm/sqlite-cloud';
import { usersTable } from './db/schema';

async function main() {
  const db = drizzle();

  const user: typeof usersTable.$inferInsert = {
    name: 'John',
    age: 30,
    email: 'john@example.com',
    phone: '123-456-7890',
  };

  await db.insert(usersTable).values(user);
  console.log('New user created!')

  const users = await db.select().from(usersTable);
  console.log('Getting all users from the database: ', users)
  /*
  const users: {
    id: number;
    name: string;
    age: number;
    email: string;
    phone: string | null;
  }[]
  */

  await db
    .update(usersTable)
    .set({
      age: 31,
    })
    .where(eq(usersTable.email, user.email));
  console.log('User info updated!')

  await db.delete(usersTable).where(eq(usersTable.email, user.email));
  console.log('User deleted!')
}

main();
```
